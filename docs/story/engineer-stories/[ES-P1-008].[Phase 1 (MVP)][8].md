### ES-P1-008 Runtime Integration and State Durability Hardening
- Config: setup localhost for all services that dont provide specific connection.
  - PostgreSQL (Neon): postgresql://neondb_owner:npg_tem0JaQWlR5y@ep-little-hall-a1njdkx1-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require.

- Task Description:
  - Replace mock runtime wiring with real Kafka integration for `jam-service`, `playback-service`, and `rt-gateway` fanout path.
  - Replace in-memory state adapters with real persistence adapters (Redis/PostgreSQL as designed) for jam queue/session, playback state, catalog lookup source, and auth token validation.
  - Restrict WebSocket origin policy to approved origins and reject unknown origins by default.
- Dependencies:
  - Kafka/Redis/PostgreSQL (Neon) environments and service credentials in non-local environments.
  - Shared contract alignment for event envelope, auth claims, and catalog validation between 
  services.
  - API gateway routing and security policy ownership from platform team.
- Definition of Done:
  - Runtime path no longer depends on noop/in-memory mocks for cross-service jam queue/playback fanout behavior.
  - Service state survives restart and preserves consistency guarantees under concurrent commands.
  - WebSocket handshake accepts only configured origins, with tests for allowlist and reject paths.
